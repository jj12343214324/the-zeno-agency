import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Zeno Insurance Agency | Personal & Commercial Insurance in California",
  description: "We don't make a living selling policies... We earn a living protecting our clients. Discover a better way to buy insurance in about 8 minutes.",
  keywords: "insurance, auto insurance, home insurance, life insurance, commercial insurance, California, Pasadena",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
